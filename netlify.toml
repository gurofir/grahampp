[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions.analyze]
  timeout = 26

[functions.interpret]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
